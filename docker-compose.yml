services:
  noga:
    build: .
    image: eladzazon/noga-whatsapp-assistant:latest
    container_name: noga-assistant
    restart: unless-stopped
    ports:
      - "${DASHBOARD_PORT:-3000}:3000"
    volumes:
      # Persistent data (SQLite + WhatsApp session)
      - noga-data:/app/data
      # Mount credentials folder
      - ./credentials:/app/credentials:ro
      # Environment file
      - ./.env:/app/.env:ro
    environment:
      - NODE_ENV=production
    networks:
      - proxy-network
    # Limit resources
    deploy:
      resources:
        limits:
          memory: 1G

volumes:
  noga-data:
    driver: local

networks:
  proxy-network:
    external: true
